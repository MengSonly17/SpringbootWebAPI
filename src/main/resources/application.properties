# Application name and server port
spring.application.name=ProductWebApi
server.port=9000

# File upload configuration
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB


# Database configuration
spring.datasource.url=jdbc:mysql://hopper.proxy.rlwy.net:41120/railway?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=aFzPHIGWuXxaObLEZGlyAcquBXZgPJUo

# HikariCP Connection Pool configuration
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.auto-commit=true

# Hibernate and JPA settings
spring.jpa.properties.hibernate.id.new_generator_mappings=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false  
spring.jpa.open-in-view=true

# Error handling (sensitive info in production should be avoided)
server.error.include-message=never

# Optional: Thymeleaf template path (uncomment if using Thymeleaf)
# spring.thymeleaf.prefix=classpath:/templates/view/

# Profile-specific configuration (optional for dev/prod setup)
# spring.profiles.active=dev  # Uncomment if you are using different profiles for dev and prod
